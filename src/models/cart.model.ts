
class Cart {
    // cartId: ObjectId;
    // userId: ObjectId;
    // products: { productId: ObjectId,productName:string,productImage:string,productPrice:number, quantity: number }[];
    // createdAt: Date;
    // updatedAt: Date;

    // constructor(cartId: ObjectId, userId: ObjectId, products: { productId: ObjectId,productName:string,productImage:string,productPrice:number, quantity: number }[], createdAt: Date, updatedAt: Date) {
    //     this.cartId = cartId;
    //     this.userId = userId;
    //     this.products = products;
    //     this.createdAt = createdAt;
    //     this.updatedAt = updatedAt;
    // }
}
export default Cart;